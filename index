<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gyan By Meera | Premium Chemistry Education</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&family=Montserrat:wght@800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #0a192f;
            --secondary: #00d2ff;
            --accent: #ff2d75;
            --light: #e6f1ff;
            --dark: #020c1b;
            --glass: rgba(10, 25, 47, 0.85);
            --transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            font-size: 100%;
            overflow-x: hidden;
        }

        body {
            font-family: 'Poppins', sans-serif;
            color: var(--light);
            background: linear-gradient(135deg, var(--dark) 0%, var(--primary) 100%);
            line-height: 1.8;
            overflow-x: hidden;
            position: relative;
            min-height: 100vh;
            padding-bottom: 120px;
            touch-action: manipulation;
            -webkit-text-size-adjust: 100%;
            -webkit-tap-highlight-color: transparent;
        }

        /* Particle Background */
        #particles-js {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: -1;
        }

        /* Navigation */
        header {
            background: var(--glass);
            backdrop-filter: blur(12px);
            padding: 1rem 5%;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 5px 30px rgba(0, 0, 0, 0.2);
            border-bottom: 1px solid rgba(0, 210, 255, 0.1);
            animation: fadeDown 0.8s ease-out;
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, var(--secondary), #0ad9c0);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0 0 20px rgba(0, 210, 255, 0.4);
            animation: pulse 2s infinite;
        }

        .logo-text {
            font-family: 'Montserrat', sans-serif;
            font-weight: 900;
            font-size: 1.5rem;
            background: linear-gradient(to right, var(--secondary), var(--light));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.5px;
        }

        .menu-toggle {
            display: none;
            background: transparent;
            border: none;
            color: var(--secondary);
            font-size: 1.8rem;
            cursor: pointer;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 1.5rem;
        }

        nav a {
            color: var(--light);
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: var(--transition);
            padding: 8px 5px;
            position: relative;
            white-space: nowrap;
        }

        nav a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 3px;
            bottom: 0;
            left: 0;
            background: var(--secondary);
            border-radius: 2px;
            transition: var(--transition);
        }

        nav a:hover {
            color: var(--secondary);
        }

        nav a:hover::after {
            width: 100%;
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 80px 5% 40px;
            position: relative;
            overflow: hidden;
        }

        .hero-content {
            max-width: 1400px;
            margin: 0 auto;
            position: relative;
            z-index: 2;
            display: flex;
            flex-direction: column;
            gap: 3rem;
            width: 100%;
        }

        .hero-text {
            animation: fadeInUp 1s ease-out 0.2s both;
            text-align: center;
            max-width: 100%;
            padding: 0 15px;
        }

        .hero h1 {
            font-size: clamp(1.8rem, 5vw, 2.8rem);
            margin-bottom: 1.5rem;
            font-weight: 900;
            line-height: 1.1;
            font-family: 'Montserrat', sans-serif;
            animation: textGlow 3s infinite alternate;
        }

        .hero h1 span {
            background: linear-gradient(to right, var(--secondary), #ff2d75);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            display: block;
            font-size: clamp(1.6rem, 4.5vw, 2.5rem);
        }

        .hero p {
            font-size: clamp(1rem, 3vw, 1.2rem);
            margin-bottom: 1rem;
            color: rgba(230, 241, 255, 0.8);
            max-width: 600px;
            line-height: 1.7;
            margin-left: auto;
            margin-right: auto;
            animation: fadeIn 2s ease-out;
        }
        
        .chemistry-quote {
            font-size: clamp(1rem, 2.5vw, 1.1rem);
            font-style: italic;
            color: rgba(230, 241, 255, 0.9);
            max-width: 600px;
            margin: 0 auto 2rem;
            padding: 15px 20px;
            border-radius: 15px;
            background: rgba(0, 210, 255, 0.1);
            position: relative;
            animation: fadeIn 3s ease-out;
            border-left: 3px solid var(--secondary);
        }

        .chemistry-quote::before {
            content: """;
            font-size: 2.5rem;
            color: var(--secondary);
            position: absolute;
            left: 10px;
            top: -15px;
            opacity: 0.5;
        }

        .hero-btns {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: linear-gradient(135deg, var(--secondary), #ff2d75);
            color: white;
            padding: 14px 30px;
            border-radius: 15px;
            text-decoration: none;
            font-weight: 700;
            font-size: clamp(0.9rem, 3vw, 1.1rem);
            transition: var(--transition);
            box-shadow: 0 8px 25px rgba(0, 210, 255, 0.4);
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            z-index: 1;
            transform: translateY(0);
            animation: float 4s ease-in-out infinite;
            flex-shrink: 0;
        }

        .btn:hover {
            transform: translateY(-8px) scale(1.05);
            box-shadow: 0 15px 40px rgba(0, 210, 255, 0.6);
        }

        .btn-outline {
            background: transparent;
            border: 2px solid var(--secondary);
            color: var(--secondary);
            box-shadow: none;
            animation-delay: 0.5s;
        }

        .btn-outline:hover {
            background: var(--secondary);
            color: var(--primary);
        }

        .hero-image {
            position: relative;
            animation: float 8s ease-in-out infinite;
            perspective: 1000px;
            max-width: 600px;
            margin: 0 auto;
        }

        .hero-image::before {
            content: '';
            position: absolute;
            width: 150%;
            height: 150%;
            top: -25%;
            left: -25%;
            background: radial-gradient(circle, rgba(0, 210, 255, 0.15) 0%, transparent 60%);
            border-radius: 50%;
            z-index: -1;
            animation: pulse 6s ease-in-out infinite;
        }

        .hero-image img {
            width: 100%;
            border-radius: 25px;
            box-shadow: 0 30px 60px rgba(2, 12, 27, 0.5);
            transform: perspective(1000px) rotateY(-15deg) rotateX(5deg);
            transition: var(--transition);
            border: 8px solid rgba(255, 255, 255, 0.05);
            filter: drop-shadow(0 15px 25px rgba(0, 210, 255, 0.3));
        }

        .hero-image:hover img {
            transform: perspective(1000px) rotateY(0) rotateX(0);
            filter: drop-shadow(0 25px 35px rgba(0, 210, 255, 0.5));
        }

        /* Sections */
        section {
            padding: 80px 5%;
            max-width: 1400px;
            margin: 0 auto;
            position: relative;
        }

        .section-title {
            text-align: center;
            margin-bottom: 60px;
            padding: 0 15px;
        }

        .section-title h2 {
            font-size: clamp(1.8rem, 5vw, 2.5rem);
            font-weight: 900;
            position: relative;
            display: inline-block;
            margin-bottom: 20px;
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(to right, var(--secondary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: textGlow 3s infinite alternate;
        }

        .section-title p {
            font-size: clamp(0.9rem, 3vw, 1.1rem);
            color: rgba(230, 241, 255, 0.7);
            max-width: 700px;
            margin: 20px auto 0;
            line-height: 1.7;
            animation: fadeIn 2s ease-out;
        }

        /* Classes Section */
        .classes-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .class-card {
            background: rgba(10, 25, 47, 0.6);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
            transition: var(--transition);
            position: relative;
            z-index: 1;
            transform: translateY(0);
            border: 1px solid rgba(0, 210, 255, 0.1);
            opacity: 0;
            animation: fadeInUp 0.6s ease-out forwards;
        }

        .class-card:nth-child(1) { animation-delay: 0.1s; }
        .class-card:nth-child(2) { animation-delay: 0.2s; }
        .class-card:nth-child(3) { animation-delay: 0.3s; }

        .class-card:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: 0 25px 50px rgba(0, 210, 255, 0.3);
            border: 1px solid rgba(0, 210, 255, 0.3);
        }

        .class-icon {
            height: 180px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 4rem;
            color: var(--secondary);
            background: linear-gradient(135deg, rgba(0, 210, 255, 0.1), transparent);
            position: relative;
            overflow: hidden;
        }

        .class-icon::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: conic-gradient(transparent, rgba(0, 210, 255, 0.1), transparent 30%);
            animation: rotate 8s linear infinite;
        }

        .class-content {
            padding: 30px;
        }

        .class-content h3 {
            font-size: clamp(1.3rem, 3vw, 1.7rem);
            margin-bottom: 15px;
            color: var(--secondary);
            font-weight: 700;
        }

        .class-content p {
            font-size: clamp(0.9rem, 2.5vw, 1rem);
            color: rgba(230, 241, 255, 0.8);
            margin-bottom: 20px;
            line-height: 1.7;
        }

        /* Syllabus Section */
        .syllabus-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
        }

        .syllabus-card {
            background: rgba(10, 25, 47, 0.6);
            backdrop-filter: blur(10px);
            padding: 40px 30px;
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
            text-align: center;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            transform: translateY(0);
            border: 1px solid rgba(0, 210, 255, 0.1);
            opacity: 0;
            animation: fadeInUp 0.6s ease-out forwards;
        }

        .syllabus-card:nth-child(1) { animation-delay: 0.1s; }
        .syllabus-card:nth-child(2) { animation-delay: 0.2s; }
        .syllabus-card:nth-child(3) { animation-delay: 0.3s; }

        .syllabus-card:hover {
            transform: translateY(-15px) scale(1.03);
            box-shadow: 0 25px 50px rgba(0, 210, 255, 0.3);
            border: 1px solid rgba(0, 210, 255, 0.3);
        }

        .syllabus-card i {
            font-size: clamp(2.5rem, 6vw, 3.5rem);
            margin-bottom: 25px;
            color: var(--secondary);
            animation: bounce 2s infinite;
        }

        .syllabus-card h3 {
            font-size: clamp(1.3rem, 4vw, 1.6rem);
            margin-bottom: 20px;
            color: var(--light);
            font-weight: 700;
        }

        .syllabus-card p {
            font-size: clamp(0.9rem, 2.5vw, 1rem);
            color: rgba(230, 241, 255, 0.8);
            line-height: 1.7;
        }

        /* Gallery Section */
        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
        }

        .gallery-item {
            height: 300px;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
            transition: var(--transition);
            position: relative;
            transform: scale(1);
            opacity: 0;
            animation: fadeIn 0.6s ease-out forwards;
        }

        .gallery-item:nth-child(1) { animation-delay: 0.1s; }
        .gallery-item:nth-child(2) { animation-delay: 0.2s; }
        .gallery-item:nth-child(3) { animation-delay: 0.3s; }
        .gallery-item:nth-child(4) { animation-delay: 0.4s; }
        .gallery-item:nth-child(5) { animation-delay: 0.5s; }

        .gallery-item:hover {
            transform: scale(1.05) rotate(2deg);
            box-shadow: 0 25px 50px rgba(0, 210, 255, 0.4);
            z-index: 2;
        }

        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
        }

        .gallery-item:hover img {
            transform: scale(1.15);
        }

        /* Contact Section */
        .contact-container {
            display: grid;
            grid-template-columns: 1fr;
            gap: 40px;
        }

        .contact-info {
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .contact-card {
            display: flex;
            gap: 20px;
            align-items: flex-start;
            background: rgba(10, 25, 47, 0.6);
            backdrop-filter: blur(10px);
            padding: 25px;
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
            transition: var(--transition);
            transform: translateY(0);
            border: 1px solid rgba(0, 210, 255, 0.1);
            opacity: 0;
            animation: fadeInUp 0.6s ease-out forwards;
        }

        .contact-card:nth-child(1) { animation-delay: 0.1s; }
        .contact-card:nth-child(2) { animation-delay: 0.2s; }
        .contact-card:nth-child(3) { animation-delay: 0.3s; }

        .contact-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 50px rgba(0, 210, 255, 0.3);
            border: 1px solid rgba(0, 210, 255, 0.3);
        }

        .contact-icon {
            width: 70px;
            height: 70px;
            background: linear-gradient(135deg, var(--secondary), #ff2d75);
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            color: white;
            flex-shrink: 0;
            box-shadow: 0 8px 25px rgba(0, 210, 255, 0.4);
        }

        .contact-text h3 {
            font-size: clamp(1.2rem, 3vw, 1.4rem);
            margin-bottom: 12px;
            color: var(--secondary);
            font-weight: 700;
        }

        .contact-text p {
            font-size: clamp(0.95rem, 2.5vw, 1.1rem);
            color: rgba(230, 241, 255, 0.8);
            line-height: 1.7;
        }

        .contact-text a {
            color: var(--secondary);
            text-decoration: none;
            transition: var(--transition);
            font-weight: 600;
        }

        .contact-text a:hover {
            color: var(--accent);
        }

        .map-container {
            height: 400px;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
            position: relative;
            transform: translateY(0);
            opacity: 0;
            animation: fadeInUp 0.6s ease-out 0.4s forwards;
            border: 1px solid rgba(0, 210, 255, 0.1);
        }

        .map-container:hover {
            box-shadow: 0 25px 50px rgba(0, 210, 255, 0.4);
            border: 1px solid rgba(0, 210, 255, 0.3);
        }

        .map-container iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        /* Review Section */
        .review-section {
            background: linear-gradient(135deg, var(--primary) 0%, #0c2039 100%);
            color: white;
            text-align: center;
            padding: 80px 5%;
            position: relative;
            overflow: hidden;
            border-radius: 25px;
            margin: 80px 0;
            border: 1px solid rgba(0, 210, 255, 0.1);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        }

        .review-section::before {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            bottom: -150px;
            left: -150px;
            background: radial-gradient(circle, rgba(0, 210, 255, 0.1) 0%, transparent 70%);
            border-radius: 50%;
            z-index: 0;
        }

        .review-content {
            position: relative;
            z-index: 2;
            max-width: 800px;
            margin: 0 auto;
        }

        .review-section h2 {
            font-size: clamp(1.8rem, 5vw, 2.5rem);
            margin-bottom: 20px;
            font-weight: 900;
            font-family: 'Montserrat', sans-serif;
        }

        .review-section p {
            font-size: clamp(0.95rem, 3vw, 1.1rem);
            max-width: 700px;
            margin: 0 auto 40px;
            color: rgba(230, 241, 255, 0.8);
            line-height: 1.7;
        }

        .rating-stars {
            display: flex;
            justify-content: center;
            gap: 12px;
            margin-bottom: 30px;
        }

        .rating-stars i {
            color: #ffd166;
            font-size: clamp(1.5rem, 4vw, 2.2rem);
            animation: bounce 1.5s infinite;
        }

        .rating-stars i:nth-child(2) { animation-delay: 0.1s; }
        .rating-stars i:nth-child(3) { animation-delay: 0.2s; }
        .rating-stars i:nth-child(4) { animation-delay: 0.3s; }
        .rating-stars i:nth-child(5) { animation-delay: 0.4s; }

        /* Contribution */
        .contribution {
            background: rgba(10, 25, 47, 0.9);
            color: rgba(230, 241, 255, 0.7);
            text-align: center;
            padding: 25px 5%;
            font-size: clamp(0.8rem, 2.5vw, 1rem);
            border-top: 1px solid rgba(0, 210, 255, 0.1);
            margin-top: 80px;
        }

        .contribution a {
            color: var(--secondary);
            text-decoration: none;
            transition: var(--transition);
            font-weight: 600;
        }

        .contribution a:hover {
            color: var(--accent);
        }

        footer {
            background: var(--dark);
            color: white;
            padding: 50px 5% 25px;
            text-align: center;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .footer-logo {
            font-family: 'Montserrat', sans-serif;
            font-size: clamp(1.8rem, 5vw, 2.2rem);
            font-weight: 900;
            background: linear-gradient(to right, var(--secondary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .footer-text {
            font-size: clamp(0.95rem, 3vw, 1.1rem);
            max-width: 700px;
            margin: 0 auto;
            color: rgba(230, 241, 255, 0.7);
            line-height: 1.7;
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 15px 0;
        }

        .social-links a {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: rgba(0, 210, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            color: var(--secondary);
            transition: var(--transition);
            text-decoration: none;
            animation: pulse 2s infinite;
        }

        .social-links a:hover {
            background: var(--secondary);
            color: var(--primary);
            transform: translateY(-8px) scale(1.1);
        }

        .copyright {
            padding-top: 30px;
            border-top: 1px solid rgba(0, 210, 255, 0.1);
            color: rgba(230, 241, 255, 0.7);
            font-size: clamp(0.8rem, 2.5vw, 0.9rem);
        }

        /* Chatbot Styles */
        .chatbot-container {
            position: fixed;
            bottom: 30px;
            right: 30px;
            z-index: 9999;
            font-family: 'Poppins', sans-serif;
        }

        .chatbot-btn {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--secondary), var(--accent));
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            cursor: pointer;
            box-shadow: 0 10px 25px rgba(0, 210, 255, 0.5);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            animation: float 3s ease-in-out infinite, pulse 2s infinite;
        }

        .chatbot-btn:hover {
            transform: scale(1.1) rotate(10deg);
        }

        .chatbot-btn::after {
            content: 'Click me to talk to Meera Bot';
            position: absolute;
            bottom: -45px;
            right: 0;
            width: 200px;
            padding: 8px 12px;
            background: rgba(10, 25, 47, 0.9);
            border-radius: 15px;
            color: var(--secondary);
            font-size: 14px;
            font-weight: 600;
            opacity: 0;
            transform: translateY(10px);
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            text-align: center;
            pointer-events: none;
        }

        .chatbot-btn:hover::after {
            opacity: 1;
            transform: translateY(0);
        }

        .chatbot-window {
            width: 350px;
            height: 500px;
            background: rgba(10, 25, 47, 0.95);
            border-radius: 25px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            transform: scale(0);
            transform-origin: bottom right;
            opacity: 0;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(0, 210, 255, 0.2);
        }

        .chatbot-window.active {
            transform: scale(1);
            opacity: 1;
        }

        .chatbot-header {
            background: linear-gradient(135deg, var(--primary), #0c2039);
            padding: 20px;
            display: flex;
            align-items: center;
            gap: 15px;
            border-bottom: 1px solid rgba(0, 210, 255, 0.1);
        }

        .chatbot-header img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            object-fit: cover;
            background: linear-gradient(135deg, var(--secondary), var(--accent));
            padding: 10px;
        }

        .chatbot-header h3 {
            font-size: 1.3rem;
            color: var(--secondary);
            font-weight: 700;
        }

        .chatbot-header p {
            font-size: 0.9rem;
            color: rgba(230, 241, 255, 0.7);
        }

        .chatbot-messages {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .message {
            max-width: 80%;
            padding: 12px 18px;
            border-radius: 18px;
            font-size: 0.95rem;
            line-height: 1.5;
            animation: messageAppear 0.3s ease-out;
            position: relative;
        }

        .bot-message {
            background: rgba(0, 210, 255, 0.15);
            border-bottom-left-radius: 5px;
            align-self: flex-start;
            color: var(--light);
        }

        .user-message {
            background: linear-gradient(135deg, var(--secondary), var(--accent));
            color: white;
            border-bottom-right-radius: 5px;
            align-self: flex-end;
        }

        .chatbot-input {
            padding: 15px;
            display: flex;
            gap: 10px;
            border-top: 1px solid rgba(0, 210, 255, 0.1);
        }

        .chatbot-input input {
            flex: 1;
            padding: 12px 18px;
            border-radius: 25px;
            border: none;
            background: rgba(0, 210, 255, 0.1);
            color: var(--light);
            font-size: 1rem;
            outline: none;
            transition: all 0.3s ease;
        }

        .chatbot-input input:focus {
            background: rgba(0, 210, 255, 0.2);
            box-shadow: 0 0 0 2px rgba(0, 210, 255, 0.3);
        }

        .chatbot-input button {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--secondary), var(--accent));
            color: white;
            border: none;
            cursor: pointer;
            font-size: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .chatbot-input button:hover {
            transform: scale(1.1);
        }

        .typing-indicator {
            display: flex;
            align-items: center;
            gap: 5px;
            padding: 12px 18px;
            background: rgba(0, 210, 255, 0.15);
            border-radius: 18px;
            align-self: flex-start;
            width: 70px;
        }

        .typing-dot {
            width: 8px;
            height: 8px;
            background: var(--secondary);
            border-radius: 50%;
            animation: typing 1.5s infinite;
        }

        .typing-dot:nth-child(2) {
            animation-delay: 0.2s;
        }

        .typing-dot:nth-child(3) {
            animation-delay: 0.4s;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes fadeDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes float {
            0% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-15px);
            }
            100% {
                transform: translateY(0px);
            }
        }

        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(0, 210, 255, 0.5);
            }
            70% {
                box-shadow: 0 0 0 15px rgba(0, 210, 255, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(0, 210, 255, 0);
            }
        }

        @keyframes rotate {
            from {
                transform: rotate(0deg);
            }
            to {
                transform: rotate(360deg);
            }
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        @keyframes messageAppear {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes typing {
            0%, 60%, 100% {
                transform: translateY(0);
            }
            30% {
                transform: translateY(-5px);
            }
        }

        @keyframes textGlow {
            0% {
                text-shadow: 0 0 10px rgba(0, 210, 255, 0.5);
            }
            100% {
                text-shadow: 0 0 20px rgba(0, 210, 255, 0.8), 0 0 30px rgba(0, 210, 255, 0.6);
            }
        }

        /* Loading Animation */
        .loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--primary);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s, visibility 0.5s;
        }

        .loader.hidden {
            opacity: 0;
            visibility: hidden;
        }

        .loader-logo {
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, var(--secondary), var(--accent));
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            margin-bottom: 30px;
            animation: pulse 2s infinite, rotate 4s linear infinite;
        }

        .loader-text {
            font-size: 1.5rem;
            color: var(--light);
            font-weight: 600;
            text-align: center;
        }

        .loader-bar {
            width: 300px;
            height: 8px;
            background: rgba(230, 241, 255, 0.1);
            border-radius: 4px;
            margin-top: 20px;
            overflow: hidden;
        }

        .loader-progress {
            height: 100%;
            width: 0%;
            background: linear-gradient(to right, var(--secondary), var(--accent));
            border-radius: 4px;
            animation: loading 2s ease-in-out forwards;
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .hero h1 {
                font-size: 2.5rem;
            }
            
            .hero h1 span {
                font-size: 2.2rem;
            }
            
            .hero p {
                font-size: 1.1rem;
            }
            
            .section-title h2 {
                font-size: 2.3rem;
            }
        }

        @media (max-width: 768px) {
            .menu-toggle {
                display: block;
            }
            
            nav ul {
                position: fixed;
                top: 80px;
                left: 0;
                width: 100%;
                background: var(--glass);
                backdrop-filter: blur(12px);
                flex-direction: column;
                gap: 0;
                text-align: center;
                height: 0;
                overflow: hidden;
                transition: all 0.4s ease;
                box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
                z-index: 1000;
            }
            
            nav ul.active {
                height: 300px;
                padding: 20px 0;
            }
            
            nav ul li {
                padding: 15px 0;
            }
            
            .hero {
                padding-top: 100px;
                height: auto;
            }
            
            .hero-content {
                flex-direction: column;
                gap: 3rem;
            }
            
            .hero-btns {
                flex-direction: column;
                align-items: center;
            }
            
            .btn {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }
            
            .section-title h2 {
                font-size: 2.2rem;
            }
            
            .contact-card {
                flex-direction: column;
                align-items: center;
                text-align: center;
            }
            
            /* Mobile Chatbot */
            .chatbot-container {
                bottom: 20px;
                right: 20px;
            }
            
            .chatbot-window {
                width: 90vw;
                height: 70vh;
                bottom: 90px;
                right: 5vw;
                left: auto;
            }
            
            .chatbot-btn::after {
                width: 180px;
                font-size: 12px;
                bottom: -40px;
            }
        }

        @media (max-width: 576px) {
            .logo-icon {
                width: 45px;
                height: 45px;
                font-size: 1.3rem;
            }
            
            .logo-text {
                font-size: 1.3rem;
            }
            
            .hero h1 {
                font-size: 2rem;
            }
            
            .hero h1 span {
                font-size: 1.8rem;
            }
            
            .hero p {
                font-size: 1rem;
            }
            
            .section-title h2 {
                font-size: 2rem;
            }
            
            .section-title p {
                font-size: 1rem;
            }
            
            .map-container {
                height: 300px;
            }
            
            .review-section h2 {
                font-size: 2rem;
            }
            
            .review-section p {
                font-size: 1rem;
            }
            
            .rating-stars i {
                font-size: 1.8rem;
            }
            
            .chatbot-btn {
                width: 60px;
                height: 60px;
                font-size: 24px;
            }
        }

        @media (max-width: 400px) {
            .hero h1 {
                font-size: 1.8rem;
            }
            
            .hero h1 span {
                font-size: 1.6rem;
            }
            
            .section-title h2 {
                font-size: 1.8rem;
            }
            
            .classes-grid,
            .syllabus-container,
            .gallery-grid {
                grid-template-columns: 1fr;
            }
        }
        
        @keyframes loading {
            to {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <!-- Loading Animation -->
    <div class="loader" id="loader">
        <div class="loader-logo">
            <i class="fas fa-atom"></i>
        </div>
        <div class="loader-text">Gyan By Meera - School of Chemistry</div>
        <div class="loader-bar">
            <div class="loader-progress"></div>
        </div>
    </div>

    <!-- Header Section -->
    <header>
        <div class="nav-container">
            <div class="logo">
                <div class="logo-icon">
                    <i class="fas fa-atom"></i>
                </div>
                <div class="logo-text">GYAN BY MEERA</div>
            </div>
            <button class="menu-toggle" id="menuToggle">
                <i class="fas fa-bars"></i>
            </button>
            <nav>
                <ul id="navMenu">
                    <li><a href="#home">Home</a></li>
                    <li><a href="#classes">Classes</a></li>
                    <li><a href="#syllabus">Syllabus</a></li>
                    <li><a href="#gallery">Gallery</a></li>
                    <li><a href="#contact">Contact</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-content">
            <div class="hero-text">
                <h1>Master Chemistry<br><span>With Expert Guidance</span></h1>
                <p>Specialized coaching for Classes 10 to +2 in Kerala, CBSE, and ICSE syllabi. Transform your understanding of chemistry with personalized teaching methods.</p>
                
                <!-- AI Generated Chemistry Quote -->
                <div class="chemistry-quote" id="chemistryQuote">
                    "Chemistry is the melodies you can play on vibrating strings of elements."
                </div>
                
                <div class="hero-btns">
                    <a href="#contact" class="btn"><i class="fas fa-book-open"></i> Enroll Now</a>
                    <a href="#syllabus" class="btn btn-outline"><i class="fas fa-graduation-cap"></i> View Syllabus</a>
                </div>
            </div>
            <div class="hero-image">
                <img src="https://images.unsplash.com/photo-1532094349884-543bc11b234d?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=800&q=80" alt="Chemistry Lab">
            </div>
        </div>
    </section>

    <!-- Classes Section -->
    <section id="classes">
        <div class="section-title">
            <h2>Our Classes</h2>
            <p>Specialized chemistry coaching for students in Classes 10 to +2 with tailored curriculum</p>
        </div>
        <div class="classes-grid">
            <div class="class-card">
                <div class="class-icon">
                    <i class="fas fa-flask"></i>
                </div>
                <div class="class-content">
                    <h3>Class 10</h3>
                    <p>Foundational chemistry concepts, periodic table, chemical reactions, acids, bases and salts, metals and non-metals with practical applications.</p>
                    <p><strong>Syllabus:</strong> Kerala, CBSE, ICSE</p>
                </div>
            </div>
            <div class="class-card">
                <div class="class-icon">
                    <i class="fas fa-vial"></i>
                </div>
                <div class="class-content">
                    <h3>Class 11</h3>
                    <p>Advanced concepts including atomic structure, chemical bonding, thermodynamics, equilibrium, redox reactions and organic chemistry basics.</p>
                    <p><strong>Syllabus:</strong> Kerala, CBSE, ICSE</p>
                </div>
            </div>
            <div class="class-card">
                <div class="class-icon">
                    <i class="fas fa-microscope"></i>
                </div>
                <div class="class-content">
                    <h3>Class 12</h3>
                    <p>Comprehensive coverage of solutions, electrochemistry, chemical kinetics, coordination compounds, organic chemistry and biomolecules.</p>
                    <p><strong>Syllabus:</strong> Kerala, CBSE, ICSE</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Syllabus Section -->
    <section id="syllabus" class="syllabus">
        <div class="section-title">
            <h2>Our Syllabus</h2>
            <p>Comprehensive coverage of all major educational boards with exam-focused teaching</p>
        </div>
        <div class="syllabus-container">
            <div class="syllabus-card">
                <i class="fas fa-school"></i>
                <h3>Kerala Syllabus</h3>
                <p>Complete coverage of Kerala State Education Board curriculum for Classes 10, 11, and 12 with focus on practical applications and board exam preparation.</p>
            </div>
            <div class="syllabus-card">
                <i class="fas fa-book"></i>
                <h3>CBSE</h3>
                <p>Thorough preparation for CBSE curriculum with NCERT-based teaching and additional resources. Special focus on competitive exam preparation.</p>
            </div>
            <div class="syllabus-card">
                <i class="fas fa-graduation-cap"></i>
                <h3>ICSE</h3>
                <p>Comprehensive ICSE syllabus coverage with emphasis on theoretical concepts and practical applications. Detailed preparation for board examinations.</p>
            </div>
        </div>
    </section>

    <!-- Gallery Section -->
    <section id="gallery" class="gallery">
        <div class="section-title">
            <h2>Our Gallery</h2>
            <p>Explore moments from our chemistry learning center</p>
        </div>
        <div class="gallery-grid">
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1622737133809-d95047b9e673?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80" alt="Chemistry Class">
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1568992688065-536aad8a12f6?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80" alt="Chemistry Experiment">
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1576097449790-4e1aa7a39702?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80" alt="Students Learning">
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1507413245164-616fafff96e6?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80" alt="Chemistry Formulas">
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1616004667892-d348f7344c78?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80" alt="Chemistry Lab">
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact">
        <div class="section-title">
            <h2>Contact Us</h2>
            <p>Get in touch for admissions and inquiries</p>
        </div>
        <div class="contact-container">
            <div class="contact-info">
                <div class="contact-card">
                    <div class="contact-icon">
                        <i class="fas fa-map-marker-alt"></i>
                    </div>
                    <div class="contact-text">
                        <h3>Our Location</h3>
                        <p>Address: Azhoor, Pathanamthitta, Kerala 689645</p>
                    </div>
                </div>
                <div class="contact-card">
                    <div class="contact-icon">
                        <i class="fas fa-phone-alt"></i>
                    </div>
                    <div class="contact-text">
                        <h3>Phone Number</h3>
                        <p><a href="tel:09495425037">094954 25037</a></p>
                    </div>
                </div>
                <div class="contact-card">
                    <div class="contact-icon">
                        <i class="fas fa-clock"></i>
                    </div>
                    <div class="contact-text">
                        <h3>Working Hours</h3>
                        <p>Monday - Saturday: 9:00 AM - 7:00 PM</p>
                        <p>Sunday: Closed</p>
                    </div>
                </div>
            </div>
            <div class="map-container">
                <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d125949.38430164625!2d76.68332325!3d9.268812049999999!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3b06183a7141e4d7%3A0x4a2d3e0d9d4c0c5f!2sAzhoor%2C%20Kerala%20689645!5e0!3m2!1sen!2sin!4v1689689268287!5m2!1sen!2sin" allowfullscreen="" loading="lazy"></iframe>
            </div>
        </div>
    </section>

    <!-- Review Section -->
    <section class="review-section">
        <div class="review-content">
            <div class="rating-stars">
                <i class="fas fa-star"></i>
                <i class="fas fa-star"></i>
                <i class="fas fa-star"></i>
                <i class="fas fa-star"></i>
                <i class="fas fa-star"></i>
            </div>
            <h2>Share Your Experience</h2>
            <p>Help us improve by sharing your feedback about our chemistry classes</p>
            <a href="https://g.co/kgs/5Hpe8Pv" target="_blank" class="btn"><i class="fas fa-pen"></i> Rate Us on Google</a>
        </div>
    </section>

    <!-- Contribution -->
    <div class="contribution">
        Website created by <a href="https://instagram.com/_shift_xd_" target="_blank">_shift_xd_</a> | A contribution from (Seventh Day Higher Secondary School's +2 Batch 2025-2026)
    </div>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="footer-logo">GYAN BY MEERA</div>
            <p class="footer-text">School of Chemistry - Specialized coaching for Classes 10 to +2 in Kerala, CBSE and ICSE syllabi</p>
            <div class="social-links">
                <a href="#"><i class="fab fa-facebook-f"></i></a>
                <a href="#"><i class="fab fa-instagram"></i></a>
                <a href="#"><i class="fab fa-whatsapp"></i></a>
                <a href="#"><i class="fab fa-youtube"></i></a>
            </div>
            <div class="copyright">
                <p>&copy; 2023 Gyan By Meera - School of Chemistry. All Rights Reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Chatbot -->
    <div class="chatbot-container">
        <div class="chatbot-btn" id="chatbotBtn">
            <i class="fas fa-robot"></i>
        </div>
        <div class="chatbot-window" id="chatbotWindow">
            <div class="chatbot-header">
                <div class="chatbot-avatar">
                    <i class="fas fa-robot"></i>
                </div>
                <div>
                    <h3>Meera Bot</h3>
                    <p>Your Chemistry Assistant</p>
                </div>
            </div>
            <div class="chatbot-messages" id="chatMessages">
                <div class="message bot-message">
                    Hello! I'm Meera Bot 🤖<br>
                    How can I help you with chemistry coaching today?
                </div>
                <div class="message bot-message">
                    Here are some things I can help with:<br>
                    - Tuition fees details<br>
                    - Syllabus information<br>
                    - Class schedules<br>
                    - Contact information<br>
                    Just ask me anything!
                </div>
            </div>
            <div class="chatbot-input">
                <input type="text" id="userInput" placeholder="Type your message here...">
                <button id="sendBtn"><i class="fas fa-paper-plane"></i></button>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
    <script>
        // Initialize loader
        window.addEventListener('load', function() {
            const loader = document.getElementById('loader');
            setTimeout(() => {
                loader.classList.add('hidden');
                
                // Initialize particles after page loads
                particlesJS('particles-js', {
                    particles: {
                        number: { value: 80, density: { enable: true, value_area: 800 } },
                        color: { value: "#00d2ff" },
                        shape: { type: "circle" },
                        opacity: { value: 0.5, random: true, anim: { enable: true, speed: 1 } },
                        size: { value: 3, random: true, anim: { enable: true, speed: 2 } },
                        line_linked: { enable: true, distance: 150, color: "#00d2ff", opacity: 0.4, width: 1 },
                        move: { enable: true, speed: 1, direction: "none", random: true }
                    },
                    interactivity: {
                        detect_on: "canvas",
                        events: { onhover: { enable: true, mode: "grab" }, onclick: { enable: true, mode: "push" } }
                    },
                    retina_detect: true
                });
            }, 1500);
        });

        // Mobile menu toggle
        const menuToggle = document.getElementById('menuToggle');
        const navMenu = document.getElementById('navMenu');
        
        menuToggle.addEventListener('click', () => {
            navMenu.classList.toggle('active');
            menuToggle.innerHTML = navMenu.classList.contains('active') ? 
                '<i class="fas fa-times"></i>' : '<i class="fas fa-bars"></i>';
        });
        
        // Close menu when clicking a link
        document.querySelectorAll('nav a').forEach(link => {
            link.addEventListener('click', () => {
                navMenu.classList.remove('active');
                menuToggle.innerHTML = '<i class="fas fa-bars"></i>';
            });
        });
        
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                window.scrollTo({
                    top: target.offsetTop - 80,
                    behavior: 'smooth'
                });
            });
        });

        // Animation on scroll
        const observerOptions = {
            threshold: 0.1
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = 1;
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe elements with animation classes
        document.querySelectorAll('.class-card, .syllabus-card, .gallery-item, .contact-card, .map-container').forEach(el => {
            observer.observe(el);
        });
        
        // Initialize chatbot
        const chatbotBtn = document.getElementById('chatbotBtn');
        const chatbotWindow = document.getElementById('chatbotWindow');
        const chatMessages = document.getElementById('chatMessages');
        const userInput = document.getElementById('userInput');
        const sendBtn = document.getElementById('sendBtn');

        // Open/close chatbot
        chatbotBtn.addEventListener('click', () => {
            chatbotWindow.classList.toggle('active');
        });

        // System prompt with tuition details
        const systemPrompt = `You are Meera Bot, an AI assistant for Gyan By Meera - School of Chemistry. 
        We offer specialized coaching for Classes 10 to +2 in Kerala, CBSE, and ICSE syllabi. 
        Our tuition fees for +2 are:
          - With extra study material: 14,000 INR
          - Without extra study material: 12,000 INR
        For other classes and details, please contact Meera Miss at 094954 25037.
        Our address is Azhoor, Pathanamthitta, Kerala 689645.
        If asked about your creator, say you were created by _shift_xd_.`;

        // Send message function
        function sendMessage() {
            const message = userInput.value.trim();
            if (message === '') return;

            // Add user message to chat
            addMessage(message, 'user');
            userInput.value = '';

            // Show typing indicator
            showTyping();

            // Simulate AI response
            setTimeout(() => {
                generateResponse(message);
            }, 1000);
        }

        // Add message to chat
        function addMessage(text, sender) {
            const messageDiv = document.createElement('div');
            messageDiv.classList.add('message', `${sender}-message`);
            if (sender === 'bot') {
                messageDiv.innerHTML = text;
            } else {
                messageDiv.textContent = text;
            }
            chatMessages.appendChild(messageDiv);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        // Show typing indicator
        function showTyping() {
            const typingDiv = document.createElement('div');
            typingDiv.classList.add('typing-indicator');
            typingDiv.innerHTML = `
                <div class="typing-dot"></div>
                <div class="typing-dot"></div>
                <div class="typing-dot"></div>
            `;
            chatMessages.appendChild(typingDiv);
            chatMessages.scrollTop = chatMessages.scrollHeight;
            
            // Remove after delay
            setTimeout(() => {
                typingDiv.remove();
            }, 2000);
        }

        // Generate response based on user input
        function generateResponse(userMessage) {
            let response = "I'm here to help with chemistry coaching information!";
            const lowerMessage = userMessage.toLowerCase();

            // Custom responses based on user input
            if (lowerMessage.includes("fee") || lowerMessage.includes("cost") || lowerMessage.includes("price")) {
                response = `Our tuition fees for +2 classes are:
                - With extra study material: 14,000 INR
                - Without extra study material: 12,000 INR
                For other classes and specific details, please contact Meera Miss at 094954 25037.`;
            } 
            else if (lowerMessage.includes("address") || lowerMessage.includes("location") || lowerMessage.includes("where")) {
                response = `We are located at:
                Azhoor, Pathanamthitta, Kerala 689645
                You can also view our location on the map in the contact section of our website.`;
            }
            else if (lowerMessage.includes("contact") || lowerMessage.includes("number") || lowerMessage.includes("phone")) {
                response = `You can contact Meera Miss directly at 094954 25037. 
                We're available Monday to Saturday, 9:00 AM to 7:00 PM.`;
            }
            else if (lowerMessage.includes("syllabus") || lowerMessage.includes("curriculum")) {
                response = `We cover all major syllabi:
                - Kerala State Board
                - CBSE
                - ICSE
                For detailed syllabus information, visit the syllabus section on our website.`;
            }
            else if (lowerMessage.includes("class") || lowerMessage.includes("course") || lowerMessage.includes("program")) {
                response = `We offer specialized chemistry coaching for:
                - Class 10
                - Class 11
                - Class 12 (+2)
                Each program is tailored to the specific syllabus and exam requirements.`;
            }
            else if (lowerMessage.includes("who made you") || lowerMessage.includes("creator") || lowerMessage.includes("_shift_xd_")) {
                response = `I was created by <a href="https://instagram.com/_shift_xd_" target="_blank">_shift_xd_</a>! 😊<br>
                        You can find me on Instagram as <a href="https://instagram.com/_shift_xd_" target="_blank">_shift_xd_</a> for more creative projects. 
                        How else can I help you with chemistry coaching?`;
            }
            else if (lowerMessage.includes("hello") || lowerMessage.includes("hi") || lowerMessage.includes("hey")) {
                response = `Hello there! I'm Meera Bot, your chemistry coaching assistant. 
                How can I help you today?`;
            }
            else if (lowerMessage.includes("thank")) {
                response = `You're welcome! 😊 
                Feel free to ask if you have any more questions about our chemistry coaching programs.`;
            }

            // Add bot response to chat
            setTimeout(() => {
                addMessage(response, 'bot');
            }, 500);
        }

        // Event listeners for chatbot
        sendBtn.addEventListener('click', sendMessage);
        userInput.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                sendMessage();
            }
        });

        // Initialize with a welcome message after a delay
        setTimeout(() => {
            addMessage("Need help with chemistry coaching? I'm here to assist!", 'bot');
        }, 3000);
        
        // AI Generated Chemistry Quotes
        const quotes = [
            "Chemistry is the study of matter, but I prefer to see it as the study of change.",
            "The universe is under no obligation to make sense to you, but chemistry helps.",
            "In all things of nature there is something of the marvelous, especially in chemistry.",
            "Chemistry can be a good and bad thing. Chemistry is good when you make love with it. Chemistry is bad when you make crack with it.",
            "Chemistry is like cooking, just don't lick the spoon.",
            "The most exciting phrase to hear in science, the one that heralds new discoveries, is not 'Eureka!' but 'That's funny...'",
            "Chemistry is necessarily an experimental science: its conclusions are drawn from data, and its principles supported by evidence from facts.",
            "Chemistry is the melodies you can play on vibrating strings of elements."
        ];
        
        function setRandomQuote() {
            const quoteElement = document.getElementById('chemistryQuote');
            const randomQuote = quotes[Math.floor(Math.random() * quotes.length)];
            quoteElement.textContent = randomQuote;
        }
        
        // Set initial quote and change every 30 seconds
        setRandomQuote();
        setInterval(setRandomQuote, 30000);
    </script>
</body>
</html>